from django.urls import path
from . import views

urlpatterns = [
   # path('', views.case_list, name='case_list'),           # كل الحالات
    #path('<int:pk>/', views.case_detail, name='case_detail'),  # حالة واحدة
]